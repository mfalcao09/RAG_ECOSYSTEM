# =====================================================================
# Sistematiza — dependências do MOTOR RAG.
# NB: necessárias apenas para `ingest`/`query`. As operações `init`,
# `normas` (import/organize/status), `status` e `doctor` rodam só com a
# stdlib (sem nada disto instalado).
#
# Recomendado: venv Python 3.11 ou 3.12 (mineru/torch podem não ter wheels
# para 3.14 ainda). Ver setup.sh.
# =====================================================================

# Núcleo: RAG-Anything (multimodal RAG sobre LightRAG). Puxa lightrag-hku,
# mineru[core], huggingface_hub, tqdm.
raganything[all]

# Backend local opcional (modo offline / privado para docs sensíveis).
ollama

# Dependências de sistema (NÃO via pip):
#   - LibreOffice  → conversão Office (.docx/.pptx/.xlsx) para PDF
#   - modelos MinerU (~1GB) baixados no 1º parse (precisa de rede uma vez)
